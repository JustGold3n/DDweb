---
import CollectionLayout from "@components/lore/CollectionLayout.astro";
import { getIndex, getGroups } from "@lib/contentParser";
import type { DocsEntry } from "@/types";
import { sortByTitle } from "@lib/sortFunctions";

const collectionIndex = (await getIndex("lore")) as DocsEntry;
const groupIndices = (await getGroups("lore", sortByTitle)) as DocsEntry[];
---

<CollectionLayout entries={groupIndices} collectionIndex={collectionIndex} />
